<?php

// Copiar este archivo a config.php y completar.
// config.php está en .gitignore — nunca subir claves reales al repositorio.

// Las claves de IA y el panel de administración viven en el chat
// (https://cadipel.pribridge.pro/admin/, ver chat/config.example.php).
// Este archivo queda en el landing solo por si algún script viejo todavía lo incluye.
